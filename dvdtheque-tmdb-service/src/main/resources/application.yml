debug: true
spring:
  application:
    name: dvdtheque-tmdb-service
  cloud:
    config:
      fail-fast: true
logging:
  level:
    root: info
    org:
        springframework: INFO  
management:
  endpoints:
    web:
      exposure:
        include: "*"