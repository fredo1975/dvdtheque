server:
  port: 8062
spring:
  application:
    name: api-gateway-server
  profiles:
    active: local
  cloud:
    config:
      fail-fast: true
    devtools:
      livereload:
        port: 0