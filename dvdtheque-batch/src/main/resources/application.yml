server:
  port: 8089
spring:
  application:
    name: dvdtheque-batch-service
  cloud:
    config:
      fail-fast: true
logging:
  level:
    root: info      
management:
  endpoints:
    web:
      exposure:
        include: "*"