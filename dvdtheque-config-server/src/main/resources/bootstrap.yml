server:
  port: 8888
spring:
  application:
    name: dvdtheque-server-config
  profiles:
    active: local
  cloud:
    config:
      label: develop
      server:
        git:
          uri: https://github.com/fredo1975/spring-cloud-server-config.git
          skipSslValidation: true
          clone-on-start: false