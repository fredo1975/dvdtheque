spring:
  application:
    name: dvdtheque-service
  cloud:
    config:
      fail-fast: true
logging:
  level:
    root: info      
management:
  endpoints:
    web:
      exposure:
        include: "*"