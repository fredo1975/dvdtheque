spring:
  application:
    name: dvdtheque-allocine-services
  cloud:
    config:
      fail-fast: true
logging:
  level:
    root: info      
management:
  endpoints:
    web:
      exposure:
        include: "*"